{% extends 'navfoot.html.twig' %}


{% block navbar %}
    {{ parent() }} 

    {% block macaron %}
    <h1>Garnissez vos coffrets de nos délicieux macarons</h1>
        <div class="container">
            <div class="row">
                <div class="d-flex flex-row justify-content-evenly my-5 flex-wrap ">                
                        {% for macaron in macarons %}         
                            <div class="card col-3 text-center chocolats-incon m-2">
                                <img src="{{asset('uploads/images/' ~ macaron.image) }}" class="card-img-top" alt="..." style=" height:300px;">
                                <div class="card-body">
                                    <h5 class="card-title">{{ macarons.nom }}</h5>
                                    <p class="card-text">{{ macaron.description }}</p>
                                    <a href="{{ path('descriptionMacaron', {'id': macaron.id }) }}" class="btn btn-light">En savoir plus</a>
                                </div>
                            </div>             
                        {% endfor %}
                    </div> 
                </div>   
            </div>
    {% endblock %}
{% endblock %}



{# 
<a href="{{ path('delete_route_name', {'id': entity.id }) }}">Delete</a>

<a href="{{ path('edit_route_name', {'id': entity.id }) }}">Edit</a> #}